enum Roles { user, admin, member, staff }

enum Correction { looading, done, error }

void main() {}
checkingroles(Roles role) {
  switch (role) {
    case Roles.user:
      break;
    case Roles.admin:
      break;
    case Roles.member:
      break;
    case Roles.staff:
      break;
  }
}
